services:
  postgres:
    image: postgres:18.4
    container_name: cluster-db
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "5432:5432"
    volumes:
      - pg_data_18:/var/lib/postgresql

volumes:
  pg_data_18: